# M1.2 Live Sync REST API Proof Design Brief

Status: Draft for review — platform decisions recorded 2026-09-06
Milestone: M1.2 Live Sync and Prototype API Proof
Companion scope map: [V1 Scope Map and Milestone Plan](V1%20Scope%20Map%20and%20Milestone%20Plan.md)
Predecessors: [M1 Rapid Thin-Slice Prototype Brief](m1-rapid-thin-slice-prototype.md) and [M1.1 Offline Multilingual Speech](m1.1-offline-multilingual-speech.md)
Foundation: [M0 SDD and Product Foundation Design Brief](m0-sdd-and-product-foundation.md)
Platform pattern reference: the `jra-sonarqube` repository (`/Users/joerice/git/joericearchitect/jra-sonarqube`), whose cold-off ECS deployment model M1.2 adopts. See [Adopted Platform Pattern](#adopted-platform-pattern).

## Purpose

Prove the narrowest end-to-end path from an installed Android Business Journal prototype to one deployed Spring Boot REST API that accepts and persists a confirmed synthetic transaction.

This is an integration proof. It may pull forward the minimum backend, Terraform, and deployment work needed for live sync, but it does not replace the full M2 infrastructure foundation or M3 deployment foundation.

## Product Outcome

An authorized tester can:

- install a signed, versioned Android prototype on a representative physical device;
- record and confirm a synthetic sale or expense while offline;
- see the transaction survive app restart as a local SQLite record;
- reconnect to the network;
- sync the queued transaction through authenticated HTTPS to one Spring Boot REST API;
- see the item marked `synced` only after the server durably accepts it; and
- retry safely without creating duplicate server records.

## Source Basis

| Source | How it informs M1.2 |
| --- | --- |
| `V1 Scope Map and Milestone Plan.md` | Defines M1.2 goal, minimum M1 carry-forward, bare-minimum M2/M3 extraction, scope, acceptance, and blocking questions. |
| `m1-rapid-thin-slice-prototype.md` | Defines the local-first Android prototype, SQLite transaction model, confirmation flow, outbox, and sync-client boundary that M1.2 connects to a live API. |
| `m1.1-offline-multilingual-speech.md` | Defines the intervening real offline speech spike. M1.2 preserves its accepted mobile behavior but does not extend or validate speech. |
| `m0-sdd-and-product-foundation.md` | Defines repository boundaries, M1.2 gates, approval requirements, synthetic-only data, and central/component OpenSpec responsibilities. |
| `define-v1-product-guardrails` OpenSpec change | Establishes accepted guardrails for offline-first behavior, human confirmation, synthetic-only data, bounded scope, external-resource approval, and follow-on architecture work. |
| `define-api-contract-conventions` OpenSpec change | Establishes accepted versioning, idempotency, error-category, and central/component ownership rules that the M1.2 sync contract must satisfy. |
| `jra-sonarqube` repository | Establishes the proven AWS account, IAM, Terraform, cold-off ECS, container-PostgreSQL, DNS/TLS, backup-gated-teardown, and cost-control pattern that M1.2 reuses. |
| `README.md` | Establishes offline-first, human-confirmed, traceable, evidence-seeking product direction. |

## Adopted Platform Pattern

Owner decision, 2026-09-06: M1.2 follows the platform pattern already proven in the `jra-sonarqube` repository rather than inventing one. That repository runs a Dockerized service and its PostgreSQL database as two containers in one ECS-on-EC2 task, cold-off by default, with Terraform-owned infrastructure and shell-script lifecycle orchestration.

### Inherited decisions

| Dimension | Decision | Source in the reference repository |
| --- | --- | --- |
| AWS account | The same existing platform account. No new account, no AWS Organizations change. | `providers.tf`, IAM bootstrap brief |
| Region | `us-east-1` | `variables.tf` |
| Human identity | One MFA-protected `joe-rice-admin` IAM user with temporary credentials via `aws login`; no long-lived access keys | IAM bootstrap brief |
| Terraform identity | A new least-privilege `hrf-platform-terraform` role/profile, scoped to this milestone's resource inventory. The reference project's `jra-platform-terraform` is not reused, because its policy was scoped to that project's inventory | `providers.tf`, IAM bootstrap brief |
| Deploy and runtime identity | `hrf-service-deploy` and `hrf-service-task`. The IAM bootstrap brief reserved `jra-mobile-backend-deploy` and `jra-mobile-backend-task` for this workload, but nothing was created, and the `hrf-` naming convention below supersedes those names at no cost. **Amended 2026-09-07:** only `hrf-service-task` is created in M1.2; `hrf-service-deploy` has no consumer here and moves to M1.4 with the GitHub trust relationship | IAM bootstrap brief permission-set matrix |
| Infrastructure as code | Terraform, state in S3 (versioned, encrypted, public access blocked) with a DynamoDB lock table | `main.tf`, `providers.tf` |
| Runtime | **ECS on EC2, not EKS.** One singleton service on an ECS cluster backed by a single EC2 host | `ecs.tf` |
| Database | **PostgreSQL as a container, not RDS**, colocated in the same task and reached over `localhost` | `app.tf` |
| Durable data | Encrypted S3 logical dumps (`pg_dump -Fc` → SHA-256 → upload → verify → `metadata/latest.txt`), with a lifecycle expiry | `backup.tf`, `scripts/backup-and-verify.sh` |
| Teardown gate | Destruction is refused unless a verified dump exists | `scripts/teardown-guard.sh` |
| DNS | Route 53, the existing registered `joericearchitect.com` apex and its existing hosted zone, plus a **new subdomain** for this API | `main.tf`, DNS/ingress brief |
| Ingress | Session-scoped DNS `A` record upserted/deleted per session, TLS terminated by a host-local Caddy proxy using Let's Encrypt DNS-01 through Route 53. No ALB. **Amended 2026-09-07:** the reference project's on-demand Elastic IP is not adopted; the record is written from the host's auto-assigned public address. See [Ingress Without An Elastic IP](#ingress-without-an-elastic-ip) | `ingress.tf`, `scripts/proxy-start.sh` |
| Admin access | Systems Manager Session Manager. No SSH, no exposed database or agent ports | `network.tf` |
| Cost control | A monthly AWS budget with an email alarm, plus cold-off as the primary control | `main.tf`, `docs/runbooks/cost.md` |
| Orchestration | Idempotent shell scripts behind a `Makefile` — `start`, `cold-stop`, `status`, `health`, `logs` | `Makefile`, `scripts/` |
| Documentation | A cost runbook and a start/stop/recover runbook, kept current with the scripts | `docs/runbooks/` |
| Working convention | One slice at a time; commit and push per slice; secrets and account identifiers stay out of committed source | reference repository conventions |

### M1.2-specific deltas from the reference

The reference deploys a third-party product for one human user through a browser. M1.2 deploys a first-party API consumed by a mobile client. That changes four things.

- **No Elasticsearch constraint, but EC2 is confirmed anyway.** The reference rejected Fargate solely because SonarQube's embedded Elasticsearch needs host-level `vm.max_map_count` and file-descriptor tuning, which a Spring Boot API does not. Fargate was therefore reconsidered on its merits and rejected on 2026-09-06; see [Why EC2 rather than Fargate](#why-ec2-rather-than-fargate).
- **Cold start is not free to the tester.** The reference accepts a 15–30 minute cold start because one person schedules their own session. M1.2's cold start gates a scripted device run, so the environment must be started deliberately before a test session and the start duration must be measured and recorded.
- **The client is a distributed retry engine, not a browser.** Cold-off means the API is normally absent. That is not a defect for M1.2; it is a free source of the controlled-outage evidence the acceptance criteria already require. See [Cost Control and the Cold-Off Lifecycle](#cost-control-and-the-cold-off-lifecycle).
- **Container images are first-party.** The reference pins a published upstream image. M1.2 must build, publish, and pin its own immutable image digest, which adds ECR and a build path the reference does not have.

### Why EC2 rather than Fargate

Owner decision, 2026-09-06. Fargate was evaluated properly rather than dismissed, because the reference project's reason for avoiding it does not apply here.

What was checked:

- **PostgreSQL runs on Fargate.** Ephemeral task storage is 20 GiB by default and up to 200 GiB, EFS is available, and ECS-managed EBS volumes are supported on Fargate platform 1.4.0 or later. Storage was not the obstacle.
- **EBS on Fargate would not have given durability anyway.** ECS documents that volumes attached to tasks managed by a *service* are never preserved and are always deleted on task termination. Durability would still have come from the S3 dump-and-restore path, exactly as it does on EC2, so the cold-off data model is unchanged by the launch type.
- **Fargate cannot hold a stable public address.** An Elastic IP cannot be associated with a Fargate task's network interface, because those interfaces are fully managed and unmodifiable. The reference project's Elastic-IP-plus-host-TLS-proxy ingress therefore has no direct equivalent; Fargate ingress means either a load balancer, which reintroduces the fixed cost this design exists to avoid, or a per-session public task IP with a TLS sidecar container.
- **Fargate would have removed real operational surface.** No AMI, no user data, no autoscaling group, no capacity provider — and with them both recovery scenarios the reference runbook records, "task stuck `PENDING` after start" and "host stuck in `Terminating:Wait` on stop".

The decision is EC2 despite that last point. The milestone's purpose is to prove an integration path, and its budget should be spent on that rather than on establishing a novel platform path, a TLS sidecar, and an in-task restore sequence at the same time. EC2 inherits a working start/stop implementation, working runbooks, and known failure modes with documented recoveries.

Recorded for the future: if the EC2 host bootstrap proves troublesome, the fallback is Fargate with a per-session public task IP and a TLS sidecar, not Fargate with a load balancer.

### Cross-project constraints this creates

Sharing an account with an existing Terraform-managed project is not free. Three collisions must be handled explicitly by the first infrastructure slice.

- **The Route 53 hosted zone is already a Terraform resource in the other project's state.** M1.2's Terraform must consume it through a data source and create only its own subdomain records. It must never declare the zone, or the two states will fight over it.
- **The AWS budget is account-wide, and the credit is shared.** The existing project sized its cap against a shared credit balance. Adding a second workload to the same account changes that arithmetic, so M1.2 needs its own budget with its own threshold and a restated combined ceiling, not a silent second tenant under the existing alarm.
- **Terraform state must be separate.** M1.2 uses its own state bucket and lock table, or at minimum its own key and lock entry, so that a mistake in one project cannot destroy the other.

## Execution Mode

Recommended profile:

`prototype-integration-proof`

Constraints:

- Synthetic data only.
- Named prototype testers only.
- No participant, production, personal, operational, or sensitive data.
- No production or pilot deployment.
- No reusable AWS credentials in the mobile app or repository.
- No unauthenticated public write endpoint.
- No broad identity, admin, reporting, loan, inventory, receipt upload, new speech/OCR capability, or AI scope.
- No multi-service architecture; one Dockerized Spring Boot modular monolith is enough.
- No EKS, no RDS, no ALB, no NAT gateway, and no always-on runtime in this milestone.
- No OpenSpec Store configuration unless a separate approved change authorizes it.

## Required Approvals Before Apply

Resolved by the owner on 2026-09-06:

- **AWS account and region** — the existing shared platform account, `us-east-1`.
- **Runtime** — ECS on EC2, not EKS and not Fargate, for cost and for reuse of a proven start/stop path. EKS remains the long-term target; see [EKS Migration Enablement](#eks-migration-enablement).
- **Database** — PostgreSQL as a container, not RDS.
- **Infrastructure as code** — Terraform.
- **DNS** — the existing Route 53 registered domain and hosted zone, with `api-dev.joericearchitect.com` as the development API hostname.
- **Cost controls** — budget alarm plus spin-up/spin-down scripts and cold-off as the primary control.
- **Deployment mechanism** — scripts for M1.2; GitHub Actions explored but not committed to. See [Deployment Path](#deployment-path).

Also resolved on 2026-09-06, in [Decisions and Open Questions](#decisions-and-open-questions): one repository holding the API with its Terraform and scripts; thin-end-to-end build order; separate Terraform state bucket and lock table; a $15 project budget alongside a $33 account-wide budget; Joe Rice as the end-to-end verification executor; automated tests only on pull requests; the development API hostname; per-tester bearer-token authentication; and unchanged EAS internal APK distribution.

Still required before Apply:

- creation of the backend repository at the decided location, and a statement of its mutation scope in the Phase A proposal;
- the API-contract detail the sync slice owns: API version, compatibility expectation, affected clients, and the stable machine-readable error identifiers required by `api-contract-conventions`; and
- the per-slice validation plan across mobile, backend, deployment, and end-to-end device sync.

## Scope

### Included

- Signed/versioned Android prototype from M1.
- Local SQLite confirmed transaction and sync outbox.
- Mobile sync-client configuration for one development API base URL.
- Named-tester authentication sufficient for synthetic prototype access.
- One Spring Boot modular-monolith REST API.
- `GET /health` or equivalent health/readiness endpoint.
- One versioned transaction-sync endpoint for confirmed manual sale and expense records.
- Idempotent create behavior using a client idempotency key.
- PostgreSQL persistence for synchronized synthetic transactions, running as a container in the same ECS task.
- Read-back evidence that an acknowledged transaction was accepted once.
- Docker image build and immutable image publication to ECR by digest.
- Development ECS cluster, task definition, and singleton service for one API workload.
- Minimal development VPC/networking needed for the API and its database container.
- Secrets Manager for API/database secrets, injected by ARN reference and never written to state or the repository.
- Public HTTPS ingress for the development API through the host's auto-assigned public address, a session-scoped Route 53 `A` record, and a host-local TLS proxy.
- Encrypted S3 database dumps with checksum verification, restore metadata, and a retention lifecycle.
- CloudWatch logs and explicit cost controls.
- Idempotent `start`, `deploy`, `cold-stop`, `status`, and `health` orchestration scripts behind a `Makefile`.
- A cost runbook and a start/stop/recover runbook.
- A documented rollback to the previous image digest and task-definition revision.
- A written assessment of what a GitHub Actions build-and-deploy path would require, without committing to build it in this milestone.

### Excluded

- Production, staging, or pilot environments.
- Production participant data or real financial data.
- EKS, Kubernetes, and Helm. The design must not obstruct a later move to EKS, but M1.2 provisions none of it.
- Amazon RDS, Multi-AZ, read replicas, or any managed database service.
- Application Load Balancer, ACM-integrated certificates, WAF, CDN, or NAT gateway.
- Always-on runtime, high availability, autoscaling beyond a single task, or any SLA.
- Self-registration, password recovery, staff roles, or full identity/profile workflows.
- HRF admin portal.
- Reports, exports, loans, inventory, supplier ordering, marketplace, or coaching workflows.
- New speech, text-to-speech, receipt capture, OCR, receipt upload, or AI-assistance work. Existing accepted M1.1 speech behavior may remain in the app but is not part of the M1.2 sync proof or acceptance evidence.
- Multi-service decomposition.
- GitOps/Argo CD.
- Mobile app store release beyond the approved prototype tester distribution path.
- Full M2/M3 infrastructure and deployment foundation beyond the bare minimum needed for this proof.

## System Shape

```text
Android prototype
  SQLite confirmed transaction
  sync_outbox item with idempotency key
  authenticated HTTPS sync client -> one configured base URL
        |
        v
api-dev subdomain (Route 53 A record, session-scoped)
        |
        v
host public IP :443 -> host-local TLS proxy (Let's Encrypt via Route 53 DNS-01)
        |
        v
ECS singleton task on one EC2 host
  +-- spring-boot-api container
  |     idempotent transaction sync endpoint
  |     health/readiness endpoint
  |     image pinned by ECR digest
  |     secrets injected from Secrets Manager by ARN
  |
  +-- postgres container (localhost:5432, shared task network)
        synthetic transaction store on a host bind mount

        | cold-stop: verified dump gate
        v
Encrypted S3 dumps + checksum + restore metadata (durable across sessions)
```

Terraform owns the account-scoped and durable infrastructure. Scripts own the session lifecycle: allocate, converge, verify, and destroy. The only state that survives a cold stop is the verified S3 dump, the Terraform state, and the ECR images.

## Cost Control and the Cold-Off Lifecycle

Cold-off is the primary cost control, exactly as in the reference project: nothing runs while idle, and a test session is a bounded `start` → run → `cold-stop` cycle.

- The durable floor is the S3 dumps and state, the ECR images, and CloudWatch logs. The hosted zone is already paid for by the existing project.
- The session cost is the EC2 host, its EBS root volume, and its public IPv4 address while the host runs — about $0.042 an hour, so dollars per month at realistic session counts, against roughly $33 a month for an always-on host and materially more for an idle EKS control plane plus RDS instance.
- `cold-stop` refuses to destroy anything until a verified database dump exists in S3. This is what makes destroying the runtime safe.
- A budget alarm is a backstop, not the control. It reports overspend after the fact; the cold-off discipline is what prevents it.

Two consequences this milestone must plan for rather than discover:

- **Persistence evidence is session-scoped.** The acceptance criterion "the API durably stores the synthetic transaction in PostgreSQL" is proved by read-back within the running session, plus the verified dump captured at cold stop. A restore-and-read-back across a full stop/start cycle is a stronger and cheap addition, and is worth including once rather than assuming.
- **The API is absent by default, and that is useful.** The acceptance criteria already demand a controlled API outage that leaves the local record intact and retryable. `cold-stop` is that outage, and `start` is the recovery. The failure path gets exercised by the operating model itself rather than by a contrived fault injection.

## EKS Migration Enablement

ECS is a cost decision, not an architectural preference. The long-term intent is EKS, so M1.2 must not accumulate choices that have to be unwound later. Migration enablement is a design constraint on this milestone, not a future project.

What makes the workload portable:

- **The deployment unit is an immutable image digest in ECR.** Identical under ECS and EKS. Never deploy a mutable tag.
- **The API is a plain twelve-factor container.** Configuration by environment variable, secrets injected by reference, logs to stdout, no writes to the container filesystem, no ECS-specific SDK calls or metadata-endpoint dependencies in application code.
- **Health and readiness are plain HTTP endpoints.** An ECS container health check and a Kubernetes probe both consume them unchanged.
- **Resource requests, environment, secret references, ports, and probe settings live in Terraform variables**, not inline in a task definition. An ECS task definition and a Kubernetes Deployment then differ in serialization, not in content.
- **Terraform is layered** — account foundations, network, runtime, workload — so a migration replaces the runtime and workload layers and leaves the rest.
- **The mobile client knows only a base URL.** No runtime detail reaches the app, so migration is invisible to it.

What will genuinely have to change at migration, recorded now so it is not a surprise:

- The colocated PostgreSQL container is a prototype economy, not a target-state pattern. At migration it becomes RDS, or an explicitly operated StatefulSet with real storage. The API must therefore reach it by a configurable host and port, never by an assumption that it is on `localhost`.
- The session public address plus host-local TLS proxy is replaced by an ingress controller or an ALB with ACM. Keeping TLS termination outside the application container is what makes that a swap rather than a rewrite.
- Host-level bootstrap in EC2 user data has no equivalent. Keeping it to nothing more than cluster registration and bind-mount preparation keeps it disposable.

The migration trigger should be stated as a threshold rather than a date: sustained multi-service need, a second team, an availability requirement the singleton cannot meet, or EKS becoming a deliberate learning objective with budget attached.

## Mobile Integration Requirements

- The app must save the confirmed transaction locally before any network activity.
- The app must create or reuse a stable outbox item with a client idempotency key.
- The sync client must call only the configured development API endpoint.
- The APK must not contain AWS keys, database credentials, production endpoints, or the tester's API credential. The credential is entered once at first launch and held in the Android keystore; the API address may be configured at build time.
- The app must call HTTPS only and must not pin the server certificate, because the development certificate is reissued each session.
- The item may move to `syncing` during the call.
- The item may move to `synced` only after the API acknowledges durable acceptance.
- Retry after timeout, network loss, or app restart must reuse the idempotency key.
- Failure must preserve the local record and show a comprehensible retryable or failed state.
- Because the development environment is cold-off by default, the app must treat a completely unreachable host as an ordinary retryable state and must not degrade into a failed or error state that requires manual intervention to clear.

## API Requirements

Minimum endpoints:

- `GET /health`
- `POST /v1/prototype/transactions:sync`

The sync endpoint accepts:

- client idempotency key;
- synthetic tester or prototype user identity;
- synthetic business identifier;
- local transaction identifier;
- transaction type: sale or expense;
- amount in minor units;
- currency code;
- occurrence date;
- category or purpose label;
- optional note;
- source type and source metadata summary; and
- client timestamps.

The sync endpoint returns:

- server transaction identifier;
- accepted status;
- server accepted timestamp;
- indication that the request was newly accepted or recognized as an idempotent replay; and
- enough read-back evidence to confirm the accepted record once.

Authentication is a per-tester bearer token presented in an `Authorization` header and validated against a short server-side list, as recorded in [Decisions and Open Questions](#decisions-and-open-questions). Tokens are issued out of band, are revocable individually, and carry a fixed expiry so the mechanism cannot quietly persist past the prototype. The server authenticates the tester identity from the token rather than trusting the tester identifier in the request body.

The API must reject unauthenticated writes, malformed payloads, unsupported transaction types, missing idempotency keys, expired or revoked tokens, and non-synthetic or unapproved data paths.

The accepted `api-contract-conventions` spec additionally requires, at proposal time and not later, that the API slice state its API version and compatibility expectation, name the affected clients, and define stable machine-readable error identifiers for validation, authentication/authorization, unsupported-operation, conflict/needs-review, idempotency-conflict, availability, and unexpected-failure categories. The brief above does not yet name those categories; the sync-contract slice owns them.

## Infrastructure and Deployment Requirements

Minimum infrastructure, defined under `iac/`. All of it is Terraform-owned except where a line says otherwise: the session-scoped ingress resources belong to the lifecycle scripts, because declaring them as infrastructure would make every plan taken during a running session report drift.

- Remote encrypted S3 state with versioning, public access blocked, and a DynamoDB lock table, separate from the existing project's state.
- One isolated development environment: VPC, one public subnet, internet gateway, and route table.
- Default-deny security groups: public inbound `443` only to the TLS proxy; the database port reachable only from the API; no SSH, agent, or database port exposed publicly.
- An ECS cluster with an EC2 capacity provider and an autoscaling group sized `min 0 / max 1 / desired 0`, with managed scaling owning the desired count.
- One task definition with the API container and the PostgreSQL container, both pinned by explicit version or digest, with health checks, awslogs configuration, and bind-mounted data directories.
- ECR repository for immutable API images, with a lifecycle policy so old images do not accumulate cost.
- Secrets Manager entries for database and API credentials, created empty by Terraform and populated out of band so no value enters state.
- The `hrf-service-task` role, least-privilege and scoped to this workload only. `hrf-service-deploy` is **not** created in M1.2: nothing here would assume it, since continuous integration makes no AWS connection and the lifecycle scripts run as `hrf-platform-terraform`. Its only purpose is to be trusted by the GitHub identity provider, so it moves to M1.4 with that work.
- An IAM grant letting the host complete Route 53 DNS-01 challenges for that hostname only. This is Terraform's only ingress resource.
- **Script-owned, not Terraform-owned:** the Route 53 `A` record for the development API subdomain. It is session-scoped, written by the lifecycle scripts from the host's auto-assigned address against the existing hosted zone consumed through a data source, and removed before the host is terminated at `cold-stop`.
- Encrypted S3 dump bucket with public access blocked and a retention lifecycle.
- CloudWatch log group for both containers.
- A monthly budget with an email alarm, and a documented shutdown rule.

Minimum deployment:

- A repeatable container build for the API, including its deterministic unit and API-contract checks.
- Immutable image publish to ECR, deployed by digest and never by a mutable tag.
- Idempotent `start`, `deploy`, `cold-stop`, `status`, and `health` scripts, each safe to re-run, each failing loudly rather than half-converging.

  **A lifecycle script must never report success on a step that failed.** This is not a general principle; it is a defect observed in the reference project on 2026-09-06. Its `cold-stop` reported success while actually erroring, so the host kept running for eleven days at $2.30 a day and the budget alarm could not see it. Two independent safeguards failed at once — the script lied, and the alarm was blind — which is why both are addressed explicitly here. Every step that can fail must propagate its failure, and `status` must report observed reality rather than the outcome the last script believed it achieved.
- Deployment metadata recorded per run: commit SHA, image digest, task-definition revision, environment, deploy result, smoke-test result, and rollback target.
- Documented rollback to the previous image digest and task-definition revision.

## Deployment Path

Owner decision, 2026-09-06: M1.2 deploys through scripts, following the reference project's `Makefile` plus `scripts/` model. GitHub Actions is explored in this milestone and decided in a later one.

### Why scripts are right for this milestone

The environment is cold-off, single-tenant, and driven by a person who is about to run a device test. A deploy is one step inside a session that a human is already conducting. Scripts also keep the whole lifecycle in one place and in one language, which is what made the reference project's start/stop path debuggable.

### What a GitHub Actions path would require

Recorded now so the later decision is informed rather than re-researched. Durable prior research exists in the reference repository under `ai-planning/research/ci-cd/`.

The split that matters is between publishing an artifact and converging an environment. These have different natural homes.

- **Build and publish is a natural fit.** Pull-request checks — Gradle build, unit tests, API-contract checks, Terraform `fmt`/`validate`/`plan` — and a main-branch job that builds once, tags by commit SHA, pushes to ECR, and records the digest. This works on GitHub-hosted runners, needs no running environment, and delivers most of the value.
- **Deploying is awkward under cold-off.** With the service normally at `desired_count 0` and no host running, a deploy job either has to start the whole environment — provisioning an EC2 host, restoring a database, publishing a DNS record, and waiting on TLS, all on a schedule nobody asked for — or it has to do nothing but publish and let the next `start` pick up the newest digest. The second is the honest answer: **let CI publish immutable digests, and let the session `start` script choose which digest to converge.**

Concrete prerequisites if it is adopted:

- An IAM OIDC provider for GitHub in the account, and a trust policy on `hrf-service-deploy` whose `sub` claim is restricted to the specific repository and branch or GitHub environment. Nothing broader.
- Explicit workflow permissions, starting at `contents: read`, with `id-token: write` granted only to the job that assumes the role.
- Pinned action versions, and no path by which fork pull requests reach the deploy role.
- Runtime secrets stay in Secrets Manager. They are not copied into GitHub secrets.
- A concurrency group so two runs cannot converge the same singleton environment at once.
- Deployment evidence published as job artifacts or a job summary, so the acceptance evidence has a durable home.

Decision, 2026-09-06: **automated tests only** for M1.2. GitHub runs the test suite on pull requests, which needs no AWS connection and no credentials. Image building and publishing stay local, driven by the scripts in `prototype-image-publish-and-lifecycle-scripts`, and environment convergence stays in scripts throughout.

The rest of the continuous-integration work is deferred to **M1.4 Prototype Continuous Integration**, defined on 2026-09-06 in the [V1 Scope Map and Milestone Plan](V1%20Scope%20Map%20and%20Milestone%20Plan.md) by splitting the runtime-agnostic half out of M3. M1.4 adds pull-request Terraform checks, the GitHub-to-AWS trust relationship, digest-addressed image publishing, and durable build evidence. It depends only on M1.2 and can run in parallel with the corpus-blocked M1.1 and M1.3. It explicitly does not deploy from continuous integration: environment convergence stays in scripts for as long as the environment is cold-off, and the two halves meet at the image digest rather than at the environment.

One consequence for M1.2: because M1.4 will need a GitHub-to-AWS trust relationship, and because that is an IAM change better reviewed once than retrofitted, `prototype-development-terraform-bootstrap` should leave room for it. Provisioning the identity provider there does not commit M1.2 to using it. The provider is account-level and therefore shared with the reference project, so it must be created once and referenced by the other, in the same way the Route 53 hosted zone is.

## Security and Data Boundaries

- Use synthetic data only.
- Use named tester access only.
- Use least privilege for workload roles, database access, Terraform state access, and any future GitHub OIDC role.
- Do not log secrets, bearer tokens, database credentials, or sensitive payloads.
- Do not store credentials in Git, Terraform state, or the APK.
- Do not create production/pilot data paths.
- Keep development data disposable and recoverable from a verified dump.
- Administer the host through Session Manager only. Expose no SSH, database, or agent port.
- Keep account identifiers and secret values out of committed source, including runbooks.

## Acceptance Criteria

- A named tester installs the signed Android APK on a representative physical device.
- The tester signs in or otherwise obtains approved prototype access.
- The tester records and confirms a synthetic sale or expense while offline.
- The confirmed transaction remains visible after app restart.
- The app shows the transaction queued for sync.
- When connectivity returns, the app sends an authenticated HTTPS request to the development REST API.
- The API durably stores the synthetic transaction in PostgreSQL.
- The app marks the transaction `synced` only after the API acknowledgement.
- Replaying the same outbox item does not create a duplicate server record.
- A controlled API outage — a `cold-stop` is sufficient — preserves the local transaction and produces a retryable or failed state.
- Restoring the API with `start` lets the same queued item sync successfully.
- The synchronized transaction is still present after a full `cold-stop` and `start` cycle, proving the dump-and-restore path rather than only in-session persistence.
- Smoke-test and deployment evidence record commit SHA, image digest, task-definition revision, endpoint, and rollback target.
- Cold-start duration is measured and recorded, so the cost of a test session is known rather than assumed.
- Validation evidence confirms that no real participant data, production endpoint, long-lived AWS credential, or reusable secret is present in the app, repository, state, logs, or deployment metadata.

## Candidate SDD/OpenSpec Changes

Propose these as separate, dependency-valid changes rather than one broad implementation change. The order below is the delivery order, and it follows from the thin-end-to-end decision recorded in [Decisions and Open Questions](#decisions-and-open-questions).

### Phase A — prove the plumbing

Phase A exists to make `api-dev.joericearchitect.com` answer a health check over HTTPS from a real device, before any sync code exists. Everything it deploys is deliberately too simple to fail for interesting reasons, so any failure in this phase is unambiguously a plumbing failure.

| Order | Change | Delivers | Checkpoint question |
| --- | --- | --- | --- |
| 1 | `prototype-development-terraform-bootstrap` | Terraform state bucket and lock table, the project and account-wide budgets, the `hrf-service-task` role, VPC and networking, and the ECR repository | **Review point.** Does the first-ever `terraform plan` match the approved resource inventory exactly? Then: does a second `plan` after `apply` return no changes? |
| 2 | `prototype-spring-boot-service-skeleton` | A Spring Boot service exposing only `GET /health`, its container build, its unit tests, and the pull-request test workflow | Do the tests pass in CI on a clean checkout, does the container build, and does `/health` answer locally? |
| 3 | `prototype-development-ecs-cluster-and-container-postgres` | ECS cluster, autoscaling group at zero, task definition, PostgreSQL container, Secrets Manager entries, encrypted S3 dump bucket, verified-dump teardown gate, and CloudWatch logs | Does the task reach RUNNING with PostgreSQL healthy, does a verified dump land in S3, and does the teardown guard refuse to proceed when no verified dump exists? |
| 4 | `prototype-image-publish-and-lifecycle-scripts` | Image build and publish by digest, the `start`, `deploy`, `cold-stop`, `status`, and `health` scripts, and the cost and start/stop/recover runbooks | **Review point.** Did `start` → healthy → `cold-stop` → `start` complete with data intact and no manual intervention, and is a second `start` against a running host a no-op? |
| 5 | `prototype-development-ingress-dns-and-tls` | Session-scoped `A` record for the development hostname written from the host's auto-assigned public address, and the host TLS proxy using Let's Encrypt DNS-01 through Route 53 | Does a real phone on a mobile network reach `https://api-dev.joericearchitect.com/health` and receive a valid response over a trusted certificate? |

All Phase A changes are backend-repository-local, inside the Phase A central envelope.

Phase A closes on one checkpoint: a real device reaches the health endpoint over HTTPS against a started session, and a `cold-stop` returns the environment to its floor.

`prototype-development-terraform-bootstrap` validates on: `terraform plan` and `apply` output; the state bucket showing versioning, encryption, and blocked public access; both budgets existing with `hrf-` prefixed names and the correct subscribers; the `hrf-service-task` role existing and scoped to this workload; the ECR repository present with its lifecycle policy; and a second `terraform plan` returning no changes, which proves the configuration is convergent rather than merely applied once.

PostgreSQL is provisioned in Phase A even though a health-only service does not need it. It shares the task definition, and provisioning it early proves the backup, verified-dump gate, and restore path before anything depends on them.

### Phase B — build the product path

| Order | Change | Repositories | Delivers | Checkpoint question |
| --- | --- | --- | --- | --- |
| 6 | `prototype-sync-contract-and-outbox` | central + mobile + backend | The versioned sync contract, its API version and compatibility statement, the stable machine-readable error identifiers required by `api-contract-conventions`, the executable API description owned by backend, and the mobile outbox wiring | Does the executable API description validate, and does the mobile client compile against the published shape? |
| 7 | `prototype-tester-authentication` | mobile + backend | Out-of-band token issuance, bearer validation on the server, Android keystore storage, and the first-run credential entry path | Is an unauthenticated write rejected, a valid token accepted, and a revoked or expired token rejected? |
| 8 | `prototype-spring-boot-api-and-synthetic-transaction-store` | backend | The transaction sync endpoint, PostgreSQL persistence, idempotent replay behavior, and read-back evidence | **Review point.** Does the endpoint behave as the contract stated, including idempotent replay producing exactly one record? |
| 9 | `prototype-android-release-delivery` | mobile | The signed APK: verified `INTERNET` permission, HTTPS-only with no certificate pinning, configured API address, and an identifiable build version | Does the signed APK install on the representative device, with `INTERNET` present and the build version visible in the app? |
| 10 | `prototype-device-live-sync-proof` | mobile + backend | The closing vertical slice — offline entry, restart survival, queued sync, replay safety, a `cold-stop` outage, and recovery on `start` | Does the full scripted device run pass end to end with evidence captured at each step? |

Authentication is delivered before the sync endpoint deliberately, so that the write endpoint is authenticated from birth rather than retrofitted. It proves itself against a trivial authenticated endpoint without waiting for the real one.

`prototype-spring-boot-service-skeleton` was split out of `prototype-spring-boot-api-and-synthetic-transaction-store` on 2026-09-06. The original single change bundled the health endpoint with the transaction store, which made the thin-end-to-end checkpoint impossible to reach: Phase A cannot deploy anything without a deployable service, and it must not deploy the sync endpoint. The skeleton carries the deployable service; the original name keeps the transaction work.

The previous `prototype-development-eks-ecr-rds-and-ingress` and `prototype-github-oidc-build-publish-and-helm-deploy` names are retired: EKS, RDS, and Helm are out of scope, and ingress has enough session-lifecycle behavior to stand alone.

`prototype-device-live-sync-proof` is the true vertical slice: it proves the Product Outcome above and is the only change whose end-to-end evidence can close M1.2.

Each component repository owns its own local OpenSpec change, implementation tasks, tests, and validation evidence. The central planning repository owns the cross-repository product contract and system acceptance evidence.

## Delivery Flow, Gates, and Checkpoints

Owner decision, 2026-09-06. M1.2 is delivered as **two central envelopes**, one per phase, rather than ten separate central changes or a single change for the milestone.

The design resolves a real tension the owner raised: fewer approvals mean cheaper delivery, but also mean problems are found later. The resolution is that approvals and checkpoints are different instruments solving different problems, and only one of them needs to be scarce.

- **Gates catch design problems** — building the wrong thing. They need human judgment and formal artifacts, so they are expensive, and they belong at contract boundaries, which means the start and end of a phase.
- **Review points catch mechanism problems** — a thing that was designed correctly but does not work. They are one pre-declared yes/no question with evidence attached, answerable in minutes. What makes them cheap is that the question is written before the work starts, not composed afterwards.
- **Checkpoints catch reality problems** — divergence between what was planned and what the world does. They are objective pass/fail, they cost the owner nothing because they notify rather than ask, and **a failed checkpoint halts the phase rather than queuing for the next gate.**

The M1 receipt failure was a reality problem: thirteen suites and 325 tests passed, then the build would not save on a physical device. No number of additional approvals would have caught that. A device checkpoint would have. That is why the frequency lives in the checkpoints rather than in the gates.

| Phase | Gates | Review points | Checkpoints |
| --- | --- | --- | --- |
| A | 2 — contract and repository creation at the start, aggregated evidence at the end | 2 — first-ever `terraform plan` before any AWS resource exists, and the cold-off lifecycle after slice 4 | 3 |
| B | 2 — contract at the start, M1.2 closing evidence at the end | 1 — the sync endpoint against its contract, before the APK ships | 4 |

Seven human decisions across ten slices, of which four require a working session and three require a look. Feedback still arrives at every slice boundary.

Two rules make this work:

1. **Every slice declares its checkpoint question before implementation begins**, as a pass/fail statement. This is the receipt-failure corrective applied per slice: the exit criterion is defined first, not discovered afterwards. The questions are recorded in the Phase A and Phase B tables above.
2. **A failed checkpoint halts the phase.** It does not accumulate for review at the next gate. Problems surface where they are created rather than where they are reviewed, which is the entire point.

Review-point placement is deliberate rather than evenly spaced. Slice 1 is where money starts and where nothing has ever existed, so its plan is worth one look. Slice 4 is where the cold-off lifecycle is first proven end to end, and it is the highest-risk mechanism in the design — the reference project's runbook documents two recovery scenarios there, a task stuck `PENDING` and a host stuck in `Terminating:Wait`. Slice 8 is where the contract meets its implementation, and changing a contract after the APK has shipped is far more expensive than changing it before.

## Preflight Before Phase A

Verified on the delivery machine, 2026-09-06: Terraform 1.15.8, AWS CLI 2.36.29, GitHub CLI 2.97.0, OpenSpec 1.8.0, and OpenJDK 25 are present, and the `jra-platform-terraform` and `jra-platform-bootstrap` profiles are configured. The following are outstanding and belong to the slices named, not to the milestone-level decisions above.

- **No container runtime is installed.** Docker, Podman, Colima, nerdctl, and Finch are all absent. Slice 2 cannot build an image and slice 4 cannot publish one until one is installed. This is the only hard blocker found.
- **Architecture and host size are resolved** as Graviton `t4g.medium`; see [Decisions and Open Questions](#decisions-and-open-questions). Slice 2 must build `linux/arm64` images and slice 3 must launch an arm64 AMI, because the build and the host have to agree.
- **Java, Spring Boot, and build-tool versions are undecided.** Pin them explicitly through a build toolchain rather than inheriting whatever the machine has installed, so continuous integration and the container build agree. OpenJDK 25 is recent enough that Spring Boot support for it should be confirmed rather than assumed. Decide in slice 2.
- **A new `hrf-platform-terraform` role must be created before slice 1's first apply**, by the account root or the temporary bootstrap role, since Terraform cannot create the role it assumes. The procedure, policies, rationale, and verification commands are written and validated in [M1.2 Terraform Role Bootstrap](../plans/m1.2-terraform-role-bootstrap.md); slice 1 relocates those documents to the service repository under `iac/bootstrap/` and captures the verification results as evidence. The reference project's `jra-platform-terraform` profile also failed to authenticate on 2026-09-06 with an expired authorization grant, which is moot given the new role but worth knowing if that project is picked up again.
- **Credit consumption measured 2026-09-06**, and it is faster than the model assumed. August usage was $17.77 and the first six days of September were $14.42, both fully offset by credits, for $32.19 consumed. Nearly all of it is a single reference-project EC2 host left running continuously since 2026-08-26 at $2.30 a day. At that rate a $200 credit beginning 2026-08-01 is exhausted around late November rather than February. Confirm the remaining balance in the Billing console, which is the only place it is exposed, and treat the runway as materially shorter than the reference project's cost runbook assumes.
- **The service repository needs OpenSpec initialized**, since it owns component-local changes under the Phase A envelope.
- **`ai-planning/evidence/m1.2/` does not exist** and is created by the first slice that produces evidence.

## Cross-Repository Delivery

M1.2 is the first milestone whose slices span repositories. It is delivered under the flow in [`cross-repository-sdd-flow.md`](../../docs/cross-repository-sdd-flow.md), which governs the mechanics below.

### Component declaration

M1.2 involves two component repositories: **mobile**, which exists, and **backend**, which does not and requires the repository-creation approvals listed under Required Approvals Before Apply.

Owner decision, 2026-09-06: the backend repository holds the Spring Boot API, its Terraform, its lifecycle scripts, and its runbooks together, following the reference project's pattern of keeping infrastructure beside the deployable. There is no separate infrastructure repository. This is to be reconsidered when a second codebase appears.

Because M1.2 still contains cross-repository slices, **milestone-autonomous delivery is unavailable for it**. Its slices are gated on handoffs between repositories that different sessions and potentially different people own. M1.2 runs one slice at a time.

### Slice repository split

The delivery order and per-slice repositories are in [Candidate SDD/OpenSpec Changes](#candidate-sddopenspec-changes). Seven of the ten changes are single-repository and carry no cross-repository overhead.

Three changes span repositories and therefore require a component declaration, an end-to-end verification assignment, and a linkage ledger from the moment their plan gate is approved:

| Change | Repositories | Why it spans |
| --- | --- | --- |
| `prototype-sync-contract-and-outbox` | central + mobile + backend | The accepted `api-contract-conventions` spec forbids the central planning repository from creating executable API descriptions and assigns them to backend, while the outbox behavior it constrains lives in mobile. A contract change excluding backend would leave the mobile client coding against a shape nothing publishes. |
| `prototype-tester-authentication` | mobile + backend | The credential is validated by the server and stored by the app; neither half is meaningful alone. |
| `prototype-device-live-sync-proof` | mobile + backend | The closing slice exercises both sides across a real network. |

Confirm each classification before proposing that change, since a change classified single-repository that turns out to span repositories will need re-proposing.

### End-to-end verification assignment

Every cross-repository slice must name its executor, environment, and evidence location before its plan gate is approved. A slice without this assignment has no path to verification and is refused at that gate. M1.2's defaults, to be confirmed or overridden per slice:

- **Executor** — Joe Rice, by owner decision on 2026-09-06, performing the device run personally. Component slices may be self-verified by their implementer. The milestone-closing slice is executed against a written step-by-step script with evidence captured per step, so that a single-person run remains reproducible by someone else later.
- **Environment** — the signed APK on a representative physical Android device, against the deployed development REST API in a started session. Explicitly **not** an emulator, and **not** the app against a stubbed or mocked API: a run that does not exercise the real service across the real network proves nothing about the sync path this milestone exists to test.
- **Evidence location** — `ai-planning/evidence/m1.2/<slice-name>.md` in this repository, referenced from the slice's linkage ledger. The directory does not exist yet and is created by the first slice that needs it.

Where a slice genuinely cannot run end to end — for example, before any development environment is provisioned — its central verification records that as an explicit residual gap rather than substituting the sum of component evidence.

### Linkage ledger

Each cross-repository slice carries a linkage ledger at `openspec/changes/<change-id>/linkage.md` in this repository, recording the contract pin and its amendment history, a dispatch entry per component repository, each return record as components close out, the end-to-end verification assignment and result, and any residual gaps. It is the durable record that lets a slice be resumed, and it is where the evidence listed under Evidence Required Before Closing M1.2 is referenced from.

## Decisions and Open Questions

### Resolved

Recorded in [Adopted Platform Pattern](#adopted-platform-pattern) on 2026-09-06: the runtime (ECS on EC2, not EKS and not Fargate), the AWS account, the DNS approach, the database model (container, not RDS), the infrastructure-as-code tool, the cost-control model, and the deployment mechanism.

Resolved by owner decision on 2026-09-06:

- **Repository layout — one repository, with Terraform segregated under `iac/`.** The Spring Boot API, its Terraform, its scripts, and its runbooks live together, following the reference project's pattern of keeping infrastructure beside the deployable. To be reconsidered when a second codebase appears. This reduces M1.2 from three component repositories to two: mobile, and backend-with-infrastructure.

  Terraform lives in an `iac/` directory rather than at the repository root, which is a deliberate divergence from the reference project. That project keeps `.tf` files at its root because it contains no application code and Terraform is the entire repository. Here the same root would hold a Spring Boot build, its sources, and a Dockerfile, so root-level `.tf` files would compete with application code for the most-read directory in the repository. Segregating them also keeps the future EKS migration legible: the runtime and workload layers to be replaced are all inside one directory.

  Expected layout:

  ```text
  build.gradle(.kts), settings.gradle, src/   Spring Boot service
  Dockerfile
  iac/                                        Terraform, layered by concern
  scripts/                                    lifecycle orchestration
  docs/runbooks/                              cost, start/stop/recover
  Makefile                                    entry points
  openspec/                                   repository-local SDD changes
  .github/workflows/                          tests only in M1.2
  ```

  `scripts/`, `docs/runbooks/`, and `Makefile` stay at the root as the reference project has them, because they are operational entry points rather than infrastructure definitions, and because the lifecycle scripts orchestrate both Terraform and the deployed service.
- **Build order — thin end to end first.** Deploy an API that answers only a health check, and reach it from a real device over HTTPS, before the sync endpoint exists. Any failure in that first pass is unambiguously a plumbing failure. The API and sync contract are developed in parallel against a local PostgreSQL container using the same image the deployed task runs. This applies the working method carried from the M1 receipt failure: prove the thin path on real infrastructure early.
- **Terraform state — its own bucket and lock table**, separate from the reference project's. Versioned, encrypted, public access blocked. The cost is negligible and it keeps a mistake in one project from reaching the other.
- **Cost control — two budgets, both configured to exclude credits.** A project budget of $15/month for attribution, plus an account-wide budget as the real guardrail, because both projects share one account and one credit. The $33 figure has a precondition, recorded 2026-09-07: at the burn rate measured on 2026-09-06, roughly $2.40 a day or a $73 monthly run rate, a $33 ceiling compared against true usage is crossed in the second week of every month by the reference project alone. It would therefore alarm permanently from the day it is created, which is the credit-blind defect arriving from the opposite direction. The reference project's idle host is stopped first, and the ceiling is then re-derived against the observed post-fix rate rather than carried forward. Budgets beyond the first two carry a small daily charge, which is accepted. Neither alarm is the control: cold-off is.

  Both budgets must set `IncludeCredit: false`. This is not a preference. Verified on 2026-09-06: the reference project's budget uses the default `IncludeCredit: true`, which subtracts credits before comparing against the limit, so its reported actual spend was $0.00 while $32 of credit had been consumed and an idle host had been running for eleven days. A budget that includes credits is structurally incapable of firing until the credit is exhausted, at which point real money starts immediately and with no warning. Excluding credits makes the same alarm report true consumption, which while credits last is exactly the credit burn rate — so one correctly configured budget answers both questions.
- **End-to-end verification executor — Joe Rice**, running the final device test personally. Component slices may be self-verified. The milestone-closing slice is executed against a written step-by-step script with evidence captured per step, so the run is reproducible by someone else later.
- **Continuous integration — automated tests only.** GitHub runs the test suite on pull requests. No AWS connection, no credentials, and no deployment automation in M1.2. Image publishing is added when the API has a stable build file; environment convergence stays in scripts. See [Deployment Path](#deployment-path).
- **Development API hostname — `api-dev.joericearchitect.com`.** The personally owned apex is accepted for synthetic prototype use. `api.` is left free for the eventual production backend, since the apex is a shared personal platform that already hosts `sonar.`. The obligation to move to a Home Roots–owned domain before any participant use is recorded alongside the mobile-repository transfer obligation.
- **Backend repository — `joericearchitect/hrf-reinvest-to-grow-service`.** Created under the owner's personal GitHub account. Ownership moves in three recorded stages rather than one: this repository is created under `joericearchitect` now; the central planning and mobile repositories transfer from `jizzoe` to `joericearchitect` later, consolidating everything under one personal account; and all repositories transfer to Home Roots ownership later still, before any participant, production, or pilot use. The project therefore spans two personal accounts until the consolidation. This is deliberate. It does not affect the GitHub-to-AWS trust relationship, whose claim is scoped per repository, but any tooling that enumerates project repositories must look in both accounts until then.

- **External-resource approval — one approval, bounded by an explicit inventory.** The owner's stated preference is as few approvals as possible, so provisioning is authorized once at the slice's plan gate rather than per resource or per `terraform apply`.

  To keep that compatible with the accepted guardrail requiring explicit approval before external resources are created, the single approval covers a named inventory of the resources the slice may create, listed in the proposal. `terraform apply` then runs without further prompting for as long as the plan matches that inventory. A plan proposing anything outside it is the stopping condition and requires a new approval. This gives one approval rather than a sequence, while keeping the approval informed rather than blanket, and it makes plan drift the thing that halts work rather than a human noticing.

- **Resource naming — `hrf-` prefix.** All resources this milestone creates are named with an `hrf-` prefix by default, so that the two projects sharing the AWS account are distinguishable by name for cost attribution, IAM scoping, and teardown safety. Exceptions are made case by case where another convention is more appropriate — globally unique S3 bucket names still need a distinguishing suffix, and the DNS hostname follows the domain rather than the prefix.

  Consequence: the `jra-mobile-backend-deploy` and `jra-mobile-backend-task` role names reserved in the reference project's IAM bootstrap brief become `hrf-service-deploy` and `hrf-service-task`. Nothing was ever created under the old names, so this rename costs nothing.

- **Runtime identities — `hrf-service-task` only in M1.2.** The lifecycle scripts run as `hrf-platform-terraform`, whose policy already carries the `ssm:SendCommand` and scoped `route53:ChangeResourceRecordSets` permissions that `start` and `cold-stop` need, so no separate deploy identity is consumed. `hrf-service-deploy` is deferred to M1.4, where the GitHub trust relationship that would use it is created.

- **Terraform identity — a new `hrf-platform-terraform` role**, scoped to this milestone's approved resource inventory rather than reusing the reference project's role, whose policy was written for that project's inventory. Because Terraform cannot create the role it assumes, the role is created out of band by the account root or the temporary `jra-platform-bootstrap` role before slice 1's first apply, and slice 1 records exactly how it was created and what it was scoped to.

- **Host architecture — Graviton (arm64).** Cheaper than x86, and it matches the arm64 delivery machine so container images build natively with no cross-compilation step. The Elasticsearch constraint that forced x86 in the reference project does not apply to a Spring Boot service.

- **Host size — `t4g.medium`, 2 vCPU and 4 GiB**, at $0.0336/hour in `us-east-1` as quoted by the AWS Pricing API on 2026-09-06.

  Sized from the expected working set rather than inherited: roughly 400-500 MiB for the operating system and ECS agent, 250-400 MiB for the PostgreSQL container against a small synthetic dataset, and 1.0-1.3 GiB for the JVM including non-heap. That is about 2.0-2.3 GiB, so 4 GiB leaves headroom for the startup peak when a database restore and JVM start overlap. `t4g.small` at 2 GiB is too tight for that peak; `t4g.large` at 8 GiB buys headroom this workload will not use.

  Burstable rather than fixed-performance because the load is bursty by nature: idle for most of a session, with spikes at startup and at each sync request. At most twenty simultaneous testers and a few hundred users a day is a trivial sustained rate, which is precisely the profile CPU credits exist for. Set the credit mode deliberately in slice 3 rather than accepting the default, so that a slow cold start is neither throttled nor silently surcharged.

  For comparison, the reference project's `m7i-flex.large` is $0.0958/hour, so this is about 65 percent cheaper per session hour.

- **Named-tester authentication — a per-tester bearer token.** Issued out of band, entered once at first launch, stored in the Android keystore, sent as `Authorization: Bearer`, validated against a short server-side list, revocable per person, and expiring on a fixed date.

  It is not an identity system: no login, password, registration, recovery, or refresh. It exists precisely because no login flow exists yet, and it substitutes for one without building one.

  A two-legged OAuth client-credentials grant giving the app a single key and secret was considered and rejected. A native app is a public client, so a secret compiled into the build ships to every device and is readable from the APK — in an Expo build, as plain text in the JavaScript bundle. The grant also requires a token endpoint, expiry, refresh, and a server-side token store, making it more machinery than a static token for weaker assurance here. Published consumer apps do not authenticate the app at all; they authenticate the user with Authorization Code plus PKCE, as prescribed by RFC 8252, because PKCE substitutes a per-login proof for the client secret a native app cannot hold. Keys that genuinely do ship inside apps, such as a Maps key, are identifiers restricted by package name and signing-certificate fingerprint, not secrets, and they authorize third-party reads rather than writes to first-party data. Proving a request came from the genuine app is a separate mechanism again — hardware-backed attestation through Play Integrity or App Attest — layered on top of user authentication rather than replacing it.

  The chosen token is on the path to M4 rather than a detour. The credential is obtained at runtime, stored in the keystore, sent as a bearer token, validated server-side, and revoked per person — all of which survive into the PKCE design that M4's `entrepreneur-registration-login` will need once many entrepreneurs each own their own records. Only the acquisition step changes, from typed-in to obtained through a login flow. A client secret compiled into the build is the one option that would be discarded entirely at M4.

  Two-legged OAuth remains correct for service-to-service calls in the final architecture: a backend calling a third party, or a scheduled job calling the API, are confidential clients that can hold secrets properly.

- **Android distribution — unchanged.** M1 already ships a signed APK through EAS internal distribution on the `preview` profile with frozen Android signing credentials, and nothing in the live-sync work changes how the APK is built, signed, or installed. Re-evaluate when the tester group grows beyond people who can be handed a file directly. The mobile slice owns four device-side changes this creates: the `INTERNET` permission must be verified rather than assumed; the app calls HTTPS only, with **no certificate pinning**, because the development certificate is reissued each session; the API address and tester credential must reach the app without the credential being baked into the build; and the installed build version must be identifiable for test evidence.

- **Ingress without an Elastic IP** *(decided 2026-09-07)*. The host uses its auto-assigned public address, and `start` writes that address into the session DNS record. The reference project allocated an Elastic IP before it had a domain or hosted zone, when a stable address was the only way to return to the same host; that reason no longer applies, because the client knows a hostname and the record is rewritten every session anyway.

  It saves nothing while running. AWS charges $0.005 an hour for every in-use public IPv4 address, elastic or auto-assigned alike, so the hourly cost is identical. What it removes is a silent leak: an unattached Elastic IP is billed at that same rate indefinitely, so a `cold-stop` that fails to release one costs about $3.65 a month for an address pointing at nothing — the same defect class as the `cold-stop` that reported success while erroring. An auto-assigned address cannot leak, because it is destroyed with the instance. It also removes two steps from `cold-stop`.

  Certificate issuance is unaffected, since DNS-01 proves ownership through a DNS record and never connects to the host.

  Trade-off: if the autoscaling group replaces the host mid-session the address changes and the record must be re-upserted, where an Elastic IP would have been re-associated. Both are script actions. A 60-second record TTL keeps the correction fast. Holding one Elastic IP permanently with a static record was also considered and rejected: it removes per-session DNS work but triples the idle floor from roughly $1.70 to $5.35 a month.

### Open

None. All M1.2 platform, delivery, and access decisions are recorded above as of 2026-09-06. The brief is decision-complete; remaining detail is settled per slice at its plan gate.

## Risks

| Risk | Mitigation |
| --- | --- |
| M1.2 becomes full platform buildout | Limit scope to one API, one synthetic transaction sync path, and one cold-off development environment. EKS, RDS, ALB, and Helm are excluded outright. |
| Infrastructure work outruns product learning | Require explicit approval and cost controls before provisioning, and keep the vertical sync proof as the only slice that can close the milestone. |
| ECS choice quietly becomes a permanent architecture | Treat migration enablement as a design constraint of this milestone, not future work. See [EKS Migration Enablement](#eks-migration-enablement). |
| Shared AWS account causes cross-project damage | Separate Terraform state, consume the shared hosted zone through a data source only, and give this workload its own least-privilege roles and its own budget. |
| Colocated PostgreSQL loses data on teardown | Gate every teardown on a verified, checksummed S3 dump, and prove restore-and-read-back across a full stop/start cycle as an acceptance criterion. |
| Live API work weakens offline-first behavior | Require local durability before network activity, preserve queued retry states, and treat an unreachable host as ordinary and retryable. |
| Cold start makes test sessions expensive in time | Measure and record cold-start duration; treat it as a known session cost rather than a surprise on test day. |
| Tester auth becomes production identity | Use provisioned prototype access only; defer registration, recovery, roles, and profiles to M4. |
| Duplicate sync creates unreliable records | Require idempotency keys and replay evidence. |
| Secrets leak into app, Git, state, or logs | Create secrets empty in Terraform and populate out of band, inject by ARN reference, use least privilege, and run explicit secret-pattern review. |
| An idle environment is left running | Cold-off by default, a documented shutdown rule, a `status` command reporting observed reality rather than the last script's belief, and a credit-visible budget alarm as the backstop. |
| A lifecycle script reports success while failing | Observed in the reference project on 2026-09-06: `cold-stop` reported success but errored, leaving a host running eleven days unnoticed because the budget alarm was simultaneously credit-blind. Every failing step must propagate; `status` must observe rather than assume; and the budget must exclude credits so the backstop is independent of the script. |

## Evidence Required Before Closing M1.2

- Installed APK version and physical-device test evidence.
- Offline local transaction and app-restart survival evidence.
- Outbox/idempotency key evidence.
- API contract test and backend unit test results.
- Terraform plan/apply evidence for the approved development foundation.
- Image build and ECR publish evidence, including the digest, produced by the lifecycle scripts.
- Phase A checkpoint evidence: a real device reaching the health endpoint over HTTPS against a started session.
- Image digest, task-definition revision, endpoint, smoke-test result, and rollback target.
- PostgreSQL persistence/read-back evidence for exactly one accepted synthetic transaction.
- Backup verification and restore evidence across a full `cold-stop` and `start` cycle.
- Replay/idempotency evidence.
- Failure/retry/recovery evidence.
- Secret-pattern, credential, endpoint, state-content, and synthetic-data-only review evidence.
- Cost alarm, cold-start duration, and shutdown-rule evidence.
- Pull-request test-run evidence from GitHub Actions for the backend test suite.
- The written GitHub Actions assessment and its recorded adoption decision.

## Next Action

Do not apply M1.2 directly from this brief. `define-cross-repository-architecture` and `define-api-contract-conventions` are both accepted, and as of 2026-09-06 the brief is decision-complete: no open questions remain.

Session entry for that work is [M1.2 Phase A Propose Handoff](../handoff-docs/m1.2-phase-a-propose-handoff.md), which carries the live account facts, the preflight state, and the five things the proposal must contain to authorize repository creation.

The proposal itself is the **Phase A envelope**, not `prototype-development-terraform-bootstrap` alone. That slice is the first change delivered inside it. It requires creation of the backend repository and explicit external-resource approval boundaries, since it is the first slice that spends money and creates AWS resources. It is also the natural decision point for any wider GitHub Actions adoption, because the GitHub-to-AWS trust relationship is an IAM change best made once.

Seven of the ten slices are single-repository. The three that span repositories — `prototype-sync-contract-and-outbox`, `prototype-tester-authentication`, and `prototype-device-live-sync-proof` — must each carry a component declaration, an end-to-end verification assignment, and a linkage ledger from the moment their plan gate is approved.
