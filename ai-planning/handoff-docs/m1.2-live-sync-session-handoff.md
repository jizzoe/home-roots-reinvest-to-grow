# M1.2 Live Sync — Session Handoff

Date: 2026-09-06
Purpose: Give a new session everything it needs to open M1.2 without re-deriving this session's decisions.
Prepared by: the session that closed M1 and rescoped receipts.

> **Superseded for session entry, 2026-09-06.** Every question this document
> listed as outstanding has since been answered. A new session opening M1.2
> should start from
> [M1.2 Phase A Propose Handoff](m1.2-phase-a-propose-handoff.md) instead. This
> document is retained because it records why M1.2 was selected and what M1
> handed forward, which the newer handoff does not repeat.

## Where the project stands

| Milestone | State |
| --- | --- |
| M0 Foundation | Complete, archived |
| M1 Rapid Thin-Slice Prototype | **Complete, archived 2026-09-06.** Manual offline entry and mocked speech proposal both delivered and accepted on the representative Android device |
| M1.1 Offline Multilingual Speech | Deferred pending audio-corpus collection |
| **M1.2 Live Sync and Prototype API Proof** | **Active next milestone** |
| M1.3 Receipt Capture, Extraction Evaluation, Reviewed Haitian Creole | Deferred pending photographed receipt corpus |

M1 closed with two of three phases delivered. All receipt work moved to M1.3.
PRD requirements REC-001 through REC-004 are unmet by M1 and explicitly owed at
V1; this is recorded in
[M1 Prototype Closure Audit](../evidence/m1-prototype-closure-audit.md).

## Why M1.2 is next

M1.1 and M1.3 are both blocked on **human data collection** — recorded speech in
three languages, and photographed receipts. That work is slow, is owned by people
outside the delivery loop, and cannot be accelerated by engineering. M1.2 is
blocked only on **decisions the owner can make**. Running the slow track in the
background while working the fast one is the reason for the ordering.

Nothing about M1.2 depends on either corpus.

## Repository state

- Central planning repository: `main`, clean, synced with origin. No active
  OpenSpec changes. `openspec validate --all --strict` passes with 6 specs.
- Mobile component repository at
  `/Users/joerice/git/joericearchitect/hrf-reinvest-in-growth/hrf-reinvest-to-grow-mobile-app`:
  `main` at `94bbf5b`, clean, synced. Single worktree. No stale branches.
- Tag `archive/m1-receipt-capture-ocr-review` preserves the discontinued receipt
  planning history, including central Gate 1 pin `77884c2`, after its branch was
  deleted.
- The JLP source documents, including the full PRD set, are now readable as
  markdown under `ai-planning/jpaul-documents/markdown/`. The originals are in
  `word-docs/`, and the PRD family reaches the repository through a Google Drive
  symlink, so a plain `find` that does not follow symlinks will miss it.

## Platform decisions, recorded 2026-09-06

The four blocking questions this handoff originally listed are largely answered.
The owner directed that M1.2 follow the pattern already proven in the
`jra-sonarqube` repository at
`/Users/joerice/git/joericearchitect/jra-sonarqube` rather than design a new one:

- **Same AWS account and region** as that project — no new account, no
  Organizations change.
- **Same Route 53 registered domain and hosted zone**, with a new subdomain for
  the development API.
- **ECS on EC2 instead of EKS**, on cost. Fargate was evaluated and rejected
  on 2026-09-06: it cannot hold a stable public address, so its ingress needs a
  load balancer or a TLS sidecar, and the milestone should not establish a novel
  platform path while proving an integration path. EKS remains the long-term target, so
  migration enablement is a design constraint on this milestone rather than
  future work.
- **Cost alarms and active cost control** — spin-up/spin-down scripts and
  cold-off by default, with PostgreSQL deployed as a container rather than RDS.
- **Terraform** for infrastructure.
- **Deployment through scripts**, with GitHub Actions explored during M1.2 and
  decided later.

That reference repository is worth reading before writing any Terraform. Its IAM
bootstrap brief already reserves `jra-mobile-backend-deploy` and
`jra-mobile-backend-task` role names for exactly this workload, and its
`docs/runbooks/` show the start/stop/recover and cost model this milestone
inherits.

Sharing an account with a live Terraform project creates three collisions the
first infrastructure slice must handle deliberately: the Route 53 hosted zone is
already a Terraform resource in the other project's state and must be consumed
through a data source rather than redeclared; the AWS credit and budget are
account-wide, so the combined ceiling has to be restated; and Terraform state
must be separated so a mistake in one project cannot destroy the other.

## Also decided 2026-09-06

- **One repository** for the API, its Terraform, its scripts, and its runbooks.
  M1.2 has two components, mobile and backend, not three. Reconsider when a
  second codebase appears.
- **Thin end to end first.** Deploy a health-check-only API and reach it from a
  real device over HTTPS before the sync endpoint exists. Develop the API and
  contract in parallel against a local PostgreSQL container.
- **Terraform state in its own bucket and lock table.**
- **Two budgets** — $15/month for this project, $33/month account-wide, since
  both projects share one account and one credit.
- **Joe Rice runs the final device test**, against a written step-by-step script
  with evidence captured per step.
- **Automated tests only** on pull requests. No AWS connection, no deployment
  automation in M1.2.

## Access and distribution decisions

**Named-tester authentication — a per-tester bearer token.** Issued out of band,
entered once at first launch, stored in the Android keystore, sent as
`Authorization: Bearer`, validated against a short server-side list, revocable
per person, and expiring on a fixed date. It is not an identity system; it
substitutes for the login flow that does not exist yet.

A two-legged OAuth client-credentials grant giving the app one key and secret was
considered and rejected. A native app is a public client, so a secret compiled
into an Expo build ships to every device and is readable as plain text in the
JavaScript bundle, and the grant needs a token endpoint, expiry, refresh, and a
token store for weaker assurance here. Published consumer apps authenticate the
user rather than the app, using Authorization Code with PKCE per RFC 8252. The
chosen token is on that path rather than a detour: runtime acquisition, keystore
storage, bearer transmission, server-side validation, and per-person revocation
all survive into M4 `entrepreneur-registration-login`, and only the acquisition
step changes.

Also decided: the hostname is `api-dev.joericearchitect.com`, leaving `api.`
free for the eventual production backend; and Android distribution stays as it
is — a signed APK through EAS internal distribution on the `preview` profile
with frozen signing credentials. Live sync changes nothing about how the APK is
built, signed, or installed, but the mobile slice owns four device-side changes:
the `INTERNET` permission must be verified rather than assumed; HTTPS only, with
no certificate pinning, because the development certificate is reissued each
session; the API address and tester credential must reach the app without baking
the credential into the build; and the installed build version must be
identifiable for test evidence.

Design brief: [M1.2 Live Sync and REST API Proof](../design-briefs/m1.2-live-sync-rest-api-proof.md).

**No blocking questions remain for M1.2 as of 2026-09-06.** The milestone is ten
slices in two phases, listed in delivery order in the roadmap and the design
brief. Phase A reaches one checkpoint — a real device reaching
`api-dev.joericearchitect.com` over HTTPS — before any sync code exists.

The next step is `prototype-development-terraform-bootstrap`, the first Phase A
slice. It requires creation of the backend repository.

Continuous integration beyond tests-only is deferred to **M1.4 Prototype
Continuous Integration**, defined 2026-09-06 by splitting the runtime-agnostic
half out of M3. M1.4 depends only on M1.2 and can run in parallel with the
corpus-blocked M1.1 and M1.3. Because M1.4 needs a GitHub-to-AWS trust
relationship, and IAM is better reviewed once than retrofitted, the Terraform
bootstrap slice should leave room for it — provisioning it does not commit M1.2
to using it. Note the identity provider is account-level and therefore shared
with the reference project, so it must be created once and referenced by the
other, exactly like the Route 53 hosted zone.

M1.2 is the first milestone that commits real money and creates external
resources. Treat every provisioning step as requiring its own explicit approval,
the way EAS builds were gated during M1.

## What M1 hands forward

M1.2 replaces a stub, not a design. The delivered prototype already has:

- A confirmed local transaction model with stable local IDs and client
  idempotency keys.
- A queued outbox shape with local, queued, synced, failed, and needs-review
  status language that never claims a remote system received a record.
- A configurable `SyncClient` boundary whose M1 implementation issues no HTTP.

M1.2 should replace that implementation without changing the confirmed
transaction model.

## Working method carried from this session

The M1 receipt slice failed twice. The second failure was diagnosed as an absent
stopping condition: "good enough" was never defined, so review kept finding new
variants and each patch grew the surface. The corrective now applied across the
prototype milestones is worth carrying into M1.2:

- Define measurable exit criteria before implementation, not after.
- Prefer a small number of hard invariants that are boolean and terminate over
  open-ended quality judgement.
- Prove the thin end-to-end path on real infrastructure early, before adding
  sophistication. For M1.2 that means one deployed endpoint accepting one
  confirmed synthetic transaction before anything else is built.
- Green automated tests are necessary and not sufficient. The discontinued
  receipt build passed 13 suites and 325 tests, then failed to save on a physical
  device.
- Push at every meaningful stopping point, and never work in a disposable
  directory. The first receipt implementation was lost to an unpushed clone under
  `/private/tmp`.

## Open obligations that are not M1.2's job

Recorded so they are not forgotten, and so no one picks them up by accident.

1. **M1.1 audio corpus.** JLP records French and Haitian Creole, Joe Rice records
   English, following the
   [M1.1 Speech Corpus Recording Guide](../plans/m1.1-speech-corpus-recording-guide.md).
   Thirty utterances per language, two passes, real speech only.
2. **M1.3 receipt corpus.** Joe Rice, before the receipt track opens. At least 30
   photographed receipts with hand-labelled ground truth. The existing synthetic
   set at `ai-planning/design-assets/sample-reciepts` is usable as text-level
   parser fixtures but not as the OCR evaluation corpus.
3. **Haitian Creole translation pass two.** Folded into M1.3 as an independent
   track. Until it completes, Creole strings must not be represented as
   human-reviewed and must not reach participants.
4. **Repository ownership consolidation, then transfer to HRF.** Decided
   2026-09-06 as a three-stage plan, not a single move:

   1. The new service repository is created as
      `joericearchitect/hrf-reinvest-to-grow-service`.
   2. At a later point, the central planning repository
      (`jizzoe/home-roots-reinvest-to-grow`) and the mobile repository
      (`jizzoe/hrf-reinvest-to-grow-mobile-app`) are transferred from `jizzoe`
      to `joericearchitect`, consolidating everything under one personal
      account.
   3. At a later point still, all repositories transfer to Home Roots
      ownership. Required before any participant, production, or pilot use.

   The project therefore spans two personal GitHub accounts until stage 2. This
   is deliberate. It does not affect the GitHub-to-AWS trust relationship, whose
   claim is scoped per repository, but it does mean any tooling that enumerates
   project repositories must look in both accounts until the consolidation
   happens.

   The development API hostname carries the same shape of obligation: it sits on
   a personally owned domain and moves to a Home Roots–owned domain before
   participant use.

## Key documents

- [V1 Scope Map and Milestone Plan](../design-briefs/V1%20Scope%20Map%20and%20Milestone%20Plan.md) — the control document
- [M1 Prototype Closure Audit](../evidence/m1-prototype-closure-audit.md) — what M1 delivered and what it left owed
- [M1 Receipt Extraction Acceptance, Evaluation, and Localization Rules](../design-briefs/m1-receipt-extraction-acceptance-and-eval-rules.md) — the exit-criteria pattern worth reusing
- [M1 Receipt Slice Reset and Fresh-Start Handoff](m1-receipt-slice-reset-and-restart-handoff.md) — why the receipt work was discontinued
